groups:
- name: fleetops-alerts
  rules:
  - alert: HighLatency
    expr: histogram_quantile(0.95, rate(http_request_latency_seconds_bucket[5m])) > 1
    for: 2m
    labels:
      severity: page
    annotations:
      summary: "High latency detected in FleetOps service"
